# Connection string for your MongoDB Atlas cloud database.
# Without this, the system will fall back to local JSON file storage (non-persistent on server crash/restart).
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/basha-restaurant?retryWrites=true&w=majority
